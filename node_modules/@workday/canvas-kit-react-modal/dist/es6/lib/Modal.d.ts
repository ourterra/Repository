import { PopupPadding } from '@workday/canvas-kit-react-popup';
import { ModalContentProps } from './ModalContent';
export declare enum ModalWidth {
    s = "440px",
    m = "800px"
}
export interface ModalProps extends Omit<ModalContentProps, 'padding' | 'width' | 'container'> {
    padding?: PopupPadding;
    width?: ModalWidth;
    container?: HTMLElement;
    open: boolean;
}
declare const Modal: {
    ({ open, padding, width, container, ...modalContentProps }: ModalProps): JSX.Element | null;
    Padding: typeof PopupPadding;
    Width: typeof ModalWidth;
};
export default Modal;
export declare function useModal(): {
    targetProps: {
        onClick(): void;
    };
    closeModal(): void;
    modalProps: {
        open: boolean;
        handleClose(): void;
    };
};
//# sourceMappingURL=Modal.d.ts.map