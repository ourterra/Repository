import * as CSS from 'csstype';
import { Prop, Length } from '../utils/types';
export interface FontSizeProps {
    fontSize?: Prop<CSS.FontSizeProperty<Length>>;
}
export declare const fontSize: import("@styled-system/core").styleFn;
