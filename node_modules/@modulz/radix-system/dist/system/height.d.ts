import * as CSS from 'csstype';
import { Prop, Length } from '../utils/types';
export interface HeightProps {
    height?: Prop<CSS.HeightProperty<Length>>;
}
export declare const height: import("@styled-system/core").styleFn;
