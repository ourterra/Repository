import * as CSS from 'csstype';
import { Prop, Length } from '../utils/types';
export interface WidthProps {
    width?: Prop<CSS.WidthProperty<Length>>;
}
export declare const width: import("@styled-system/core").styleFn;
