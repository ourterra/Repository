import * as CSS from 'csstype';
import { Prop, Length } from '../utils/types';
export interface MaxWidthProps {
    maxWidth?: Prop<CSS.MaxWidthProperty<Length>>;
}
export declare const maxWidth: import("@styled-system/core").styleFn;
