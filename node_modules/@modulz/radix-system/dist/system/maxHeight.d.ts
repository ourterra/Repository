import * as CSS from 'csstype';
import { Prop, Length } from '../utils/types';
export interface MaxHeightProps {
    maxHeight?: Prop<CSS.MaxHeightProperty<Length>>;
}
export declare const maxHeight: import("@styled-system/core").styleFn;
