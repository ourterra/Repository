import * as CSS from 'csstype';
import { Prop, Length } from '../utils/types';
export interface MinHeightProps {
    minHeight?: Prop<CSS.MinHeightProperty<Length>>;
}
export declare const minHeight: import("@styled-system/core").styleFn;
