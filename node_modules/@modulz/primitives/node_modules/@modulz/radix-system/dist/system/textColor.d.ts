import * as CSS from 'csstype';
import { Prop } from '../utils/types';
export interface TextColorProps {
    textColor?: Prop<CSS.ColorProperty>;
}
export declare const textColor: import("@styled-system/core").styleFn;
